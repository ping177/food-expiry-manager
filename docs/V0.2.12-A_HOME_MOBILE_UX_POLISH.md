# v0.2.12-A 首页 Mobile UX Polish

## 目标

将登录信息从库存首页顶部迁移到“我的”页面，并在两个顶层页面之间提供固定的双 Tab 导航：库存、我的。

## 范围

- 库存页保留商品列表、搜索、筛选和添加商品入口。
- 我的页展示当前账号状态、脱敏邮箱、退出登录和三个未来设置占位项。
- 添加商品、库存详情和编辑属于任务流，不显示底部 Tab 导航。
- 底部导航固定，并为移动端安全区和内容底部留白。

## 非目标

- 不新增第三个 Tab。
- 不修改 Supabase Auth flow、schema、RLS、环境变量、库存数据模型或库存 API。
- 不实现提醒设置、数据导出或偏好设置页面。

## 验证

- `npm test`
- `npm run build`
- `git diff --check`
